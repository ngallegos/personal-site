@model List<PersonalSite.ContentModel.Link>

@if (Model?.Any() ?? false)
{
    <ul class="flex flex-wrap justify-between flex-row md:flex-col">
        @foreach(var link in Model)
        {
            <li><a href="@link.Slug" class="nav mx-2 md:mx-0">@link.Text</a></li>
        }
    </ul>
}