@page
@using PersonalSite.ContentModel
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div>
    
    @* @if (Model.HomePage?.FeaturedImage != null) *@
    @* { *@
    @*     var image = Model.HomePage.FeaturedImage; *@
    @*     <img src="@image.File.Url" alt="@image.Description"/> *@
    @* } *@
    @Model.HomePage?.HtmlContent
    
</div>